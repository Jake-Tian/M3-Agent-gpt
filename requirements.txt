# Core
opencv-python-headless
pillow
matplotlib
scikit-learn
tqdm
openai

# Video/Audio
moviepy==2.1.2
pydub==0.25.1
httpx==0.23.0

# PyTorch
torch==2.6.0
torchvision==0.21.0
torchaudio==2.6.0

# ML / Transformers
accelerate==0.34.2
peft==0.15.2
trl==0.16.0
setuptools_scm
torchdiffeq
resampy
x_transformers

# Build
ninja

# Jupyter
jupyter

# Other
onnxruntime==1.22.1
insightface==0.7.3
hdbscan==0.8.40

# Install flash-attn separately if needed: pip install flash-attn==2.6.3 --no-build-isolation
